---
description: TypeScript 代码规范——严格模式、Zod 优先、禁止 any、异步规范。
globs: ["**/*.ts", "**/*.tsx"]
alwaysApply: false
---

# TypeScript 代码规范

## 严格模式要求

```json
// tsconfig.json 必须包含
{
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true
  }
}
```

## 类型定义规范

```typescript
// ✅ 用 Zod 定义 schema，类型从 schema 推导
import { z } from 'zod'

export const CreateProjectSchema = z.object({
  inputMode: z.enum(['prompt', 'article', 'outline']),
  rawInput: z.string().min(1).max(2000),
  durationPreset: z.enum(['30s', '1min', '2min', '5min']),
  aspectRatio: z.enum(['16:9', '9:16', '1:1']),
})

export type CreateProjectInput = z.infer<typeof CreateProjectSchema>

// ❌ 禁止手写 interface 然后手写验证逻辑
// ❌ 禁止使用 any
```

## 异步规范

```typescript
// ✅ 明确错误类型
async function generateScript(input: CreateProjectInput): Promise<Scene[]> {
  try {
    const result = await claudeClient.messages.create(...)
    return parseScenes(result)
  } catch (error) {
    if (error instanceof Anthropic.APIError) {
      throw new AppError('CLAUDE_API_ERROR', error.message, { status: 502 })
    }
    throw error
  }
}

// ❌ 禁止空 catch
// ❌ 禁止 catch (e: any)
```

## 导入规范

```typescript
// ✅ 使用路径别名，不用相对路径 ../../
import { db } from '@vid-maker/db'
import { CreateProjectSchema } from '@vid-maker/shared'

// ✅ 类型导入用 import type
import type { Project } from '@vid-maker/shared'
```

## 命名规范

- 文件名：`kebab-case.ts`
- 类/接口：`PascalCase`
- 变量/函数：`camelCase`
- 常量：`SCREAMING_SNAKE_CASE`
- Zod schema：`PascalCaseSchema`（如 `CreateProjectSchema`）
